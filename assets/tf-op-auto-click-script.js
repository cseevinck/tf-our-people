'use strict';

/**
 * Script to process an automatic click to get the page to populate 
 * (See enqueue_auto_click_script in tf-our-people.php)
 */

  jQuery(document).ready(function () {
    jQuery('.user_like').click();
  });

  